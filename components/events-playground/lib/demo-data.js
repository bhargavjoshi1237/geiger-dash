// The landing playground runs on the screen's own demo data, so the Events data
// layer is stubbed out here — nothing on the marketing page touches Supabase.
const none = async () => [];

export const listEvents = none;
export const listProjectOrders = none;
export const listRegistrations = none;
export const listAttendanceByProject = none;
export const listOrderRefunds = none;
export const listDisputes = none;
