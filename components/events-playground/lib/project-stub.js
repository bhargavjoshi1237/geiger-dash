// Playground stand-in for the workspace's project context: there is no signed-in
// project on the landing page, so every screen reads as project-less and renders
// its demo state.
export function useOptionalProject() {
  return null;
}
